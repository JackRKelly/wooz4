query GetCollectionById($id: ID!) {
	collection(id: $id) {
		__typename
		title
		description
		image {
			transformedSrc
		}
		products(first: 8) {
			edges {
				node {
					__typename
					id
					title
					handle
					images(first: 1) {
						edges {
							node {
								id
								transformedSrc
							}
						}
					}
					priceRange {
						minVariantPrice {
							amount
							currencyCode
						}
					}
				}
			}
		}
	}
}
